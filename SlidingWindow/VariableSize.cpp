## Template for Variable Size Slding window

    int i=0,j=0;
    type k;                 ** Here k deontes some condition . 
/*
    while(j < arr.size())
    {
        ** Calculations 
        if(cond == k)
            ** Determine the Answer from Calculations.

        else
        {
            ** Basically here we are compressing the window.

            while(cond > k)
                ** Slide the Window.
        }
        j++;
    }
    return res;
*/
