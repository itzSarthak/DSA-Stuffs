
// Move all zeroes to the end of the array
#include <bits/stdc++.h>
using namespace std;

int main()
{
    // very easy 
    /*
        **code
        int i=0,j=0;
        while(j < nums.size())
        {
            if(nums[j])
                swap(nums[i++],nums[j]);

            j++;
        }
    */
    return 0;       
}